<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.example.test.mapper.UserMapper">

    <select id="getInfo" resultType="com.example.test.bean.UserBean">
        SELECT * FROM user WHERE name = #{name}
    </select>

   <insert id="getinfo2" parameterType="com.example.test.bean.UserBean">
       INSERT INTO user (name,password,tel,sex) VALUES(#{name},#{password},#{tel},#{sex})
   </insert>

</mapper>